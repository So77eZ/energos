# Замечания и предложения по проекту Energos

## Актуальные задачи

### 🔴 Высокий приоритет

- **Изображения не отображаются на мобильных устройствах** — при открытии сайта с телефона картинки энергетиков не загружаются.

### 🟡 Средний приоритет

- **Сортировка по цене** — в фильтре не хватает сортировки по дороговизне.
- **Сортировка и фильтрация отзывов** по рейтингу и дате.
- **Расширенный фильтр** — всплывающее окно с фильтрацией по параметрам напитка (сладость, кислотность и т.д.), а не только сортировка.
- **Полнотекстовый поиск** — нечёткий поиск через `pg_trgm` (монстер / монстэр / monster).
- **Добавить способ обратной связи** — ссылка на тг-бота или личку.
- **Карта вкусов: выбор осей X/Y** — сейчас отображаются только 2 метрики из 6.
- **`loading.tsx` / Suspense-границы** — Next.js App Router поддерживает стриминг: страницы могут показывать скелетон пока данные грузятся, вместо белого экрана. Сейчас ни одного `loading.tsx` нет.
- **`not-found.tsx`** — если передать несуществующий `?id=` в адресе, страница молча показывает первый напиток. Нужна явная 404-страница для отсутствующих напитков.
- **Бренд напитка как отдельное поле в БД** — сейчас фронт берёт первый ALL-CAPS токен из имени (`RED BULL Red Edition` → бренд `RED BULL`), fallback `ENERGOS`. С нормальным полем `brand` всё было бы стабильнее и можно фильтровать.
- **Объём напитка как отдельное поле** — `0.45 л`, `0.25 л` сидит в имени. `cleanDrinkName()` его выкидывает, но потерянная информация полезная — фильтр по объёму и группировка дублей по объёму невозможны.
- **Описание/blurb напитка** — поле для hero/карточки, 1–2 предложения о вкусе/составе. Сейчас место в дизайне есть, но в БД пусто.

### 🟢 Низкий приоритет / По желанию

- **3D-банки по бокам каталога (десктоп)** — два вращающихся лоу-поли энергетика в свободных зонах слева и справа от сетки карточек.

  **Стек:** `@react-three/fiber` + `three`, lazy-import через `next/dynamic({ ssr: false })`, только на `lg:` breakpoint (~150 KB gzip).

  **Реализация:**
  - Два отдельных `<canvas>` с `position: fixed`, прозрачный фон; занимают пространство от края viewport до основного контейнера.
  - Z-index: банки `z-0`, контент `z-10`, навбар `z-50`, кнопка «наверх» — выше банок.
  - Банка: `CylinderGeometry(r, r, h, 8)` + `flatShading: true`, 2–3 цветовых полосы (тёмно-синий + акцент сайта). Левая: наклон Z +15°, правая: Z −12°; разные фазы покачивания и скорости вращения.
  - Кубики льда (4–5 на банку): `BoxGeometry` + `opacity: 0.45`, каждый на своём радиусе орбиты, угловой скорости и наклоне плоскости — как кольцо Сатурна. Плюс вращение вокруг своей оси по всем трём осям.

  **Улучшение после:** заменить абстрактные банки на 3D топ-2 напитка по среднему рейтингу (брать с бэкенда).

- **Сканирование штрих-кода банки → переход на карточку** — пользователь подносит банку к камере телефона, приложение распознаёт EAN-13, ищет напиток в нашей базе и редиректит на `/drinks?id=N`. Если не нашли — предлагает оставить заявку на добавление (см. соседний пункт «Заявки на добавление энергетиков»).

  **Бэк:**
  - Миграция: новая колонка `barcode: str | None` в `energy_drinks`, уникальный partial-index `WHERE barcode IS NOT NULL` (один EAN — одна позиция; nullable потому что для большинства старых импортов будет пусто пока не добьём вручную).
  - `GET /api/energy-drinks/by-barcode/{ean}` — 200 `EnergyDrinkSchema` или 404. EAN валидируется регуляркой `^\d{8,14}$` (EAN-8/EAN-13/UPC-A/ITF-14).
  - В `CreateEnergyDrinkSchema` / `UpdateEnergyDrinkSchema` добавить опциональное поле `barcode`. Для админки.
  - При импорте из DNS подтянуть `barcode` если он есть в источнике (DNS отдаёт его на карточке товара).

  **Фронт:**
  - Кнопка «📷 Сканировать» в шапке (рядом с поиском) или floating action на мобиле (камера полезнее в стоячем магазине, чем за десктопом).
  - Библиотека: `@zxing/library` (~50 КБ gz, ES-модули, открывает камеру через `navigator.mediaDevices.getUserMedia`, читает EAN-13/UPC/Code128). Альтернатива — `html5-qrcode` с готовым UI, но добавляет ~40 КБ за приятный intro-фрейм. Я бы взял zxing напрямую и сверстал свой viewfinder.
  - HTTPS обязательно — без него Camera API заблокирован браузером. У нас Caddy в проде это даёт; локально работает только на `localhost` или через ngrok.
  - Флоу:
    1. Клик «Сканировать» → modal-overlay с `<video>` (BackCamera через `facingMode: 'environment'`).
    2. Полупрозрачная рамка-ROI в центре + подсказка «Наведите штрих-код в рамку».
    3. zxing-loop на каждом кадре → при первом успехе: бип (`new Audio('...')`) + лёгкая вибрация (`navigator.vibrate(50)`).
    4. Запрос на `GET /api/energy-drinks/by-barcode/{ean}` → success → `router.push('/drinks?id=' + id)`.
    5. 404 → показать «Этот напиток не в каталоге» + CTA «Предложить добавить» (откроет форму заявки с уже подставленным EAN).
  - Fallback для desktop / без камеры / при отказе в permission: ручной input «Ввести код вручную».
  - Edge cases: одна банка разных объёмов (0.25/0.5 л) — у каждой свой EAN, в БД будут разные `id`. Это ОК — каждый EAN ведёт на свою позицию.
  - Опционально (улучшение): после успешного скана засчитывать «реальное взаимодействие» в счётчик активности юзера → влияет на ачивки (если когда-то сделаем геймификацию).

- **Избранные энергетики** — список любимых напитков в профиле пользователя с кнопкой добавления в избранное прямо с карточки.
- **Слайдер-переключатель в профиле** — переключение между вкладками «Избранные» и «Отзывы» вместо единого списка.
- **Иконка редактирования отзыва в профиле** — добавить кнопку-карандаш прямо в карточку отзыва в профиле пользователя.
- **Убрать дубли напитков по объёму** — одинаковые напитки разного объёма (например 0.25 и 0.5 л) отображаются как отдельные позиции; нужна дедупликация или группировка.
- **Обрезать префикс «Напиток энергетический» при парсинге** — при импорте товаров убирать категорийный префикс из названия.
- **Заявки на добавление энергетиков** — форма с названием + опциональной ссылкой на референс.
- **Фильтр по кол-ву отзывов** - сделать фильтр по количеству отзывов на энергетик от большего к меньшему.
- **Сравнение напитков** — режим side-by-side по всем метрикам. Тогглер «Только различающиеся» — скрывать строки с одинаковыми значениями у всех сравниваемых напитков (как у DNS).
- **Сброс пароля / верификация email** — стандарт для пользовательских платформ.
- **Кэш на фронте** — React Query или SWR вместо прямых fetch.
- **Англо-русский словарь** — «бэрн» → предлагать «Burn» при схожести >90%.
- **Парсинг актуальных цен** — внешний источник данных.
- **Вебсокет для обновления цен** — по приколу, real-time цены.
- **Индикатор активности рецензента** — на карточке отзыва отображать число отзывов автора; цвет числа интерполируется от бледно-зелёного (новичок) до ярко-красного (топ по количеству отзывов) пропорционально позиции автора среди всех пользователей. При наведении — ненавязчивый тултип с пояснением.
- **Лидерборд рецензентов** — рейтинговая таблица пользователей, отсортированных по количеству оставленных отзывов.
- **Реакции эмодзи на отзыв** — бэк готов (коммит `c6bcbbe`, миграция `78632180e43a_add_review_emoji_model`):
  - Таблица `review_emojis` с UniqueConstraint `(review_id, user_id, emoji_unicode)` — один и тот же эмодзи нельзя поставить дважды от одного юзера, но разные можно
  - `GET /api/reviews/{review_id}/emojis/` — список всех реакций на отзыв (публично)
  - `POST /api/reviews/{review_id}/emojis/?emoji=🤙` — добавить (требует auth, любой unicode-символ)
  - `DELETE /api/reviews/{review_id}/emojis/?emoji=🤙` — убрать свою (требует auth)
  - Схема ответа: `{ id, emoji_unicode, review_id, user_id, created_at, updated_at }`
  - Фронт: под каждым отзывом ряд агрегированных реакций «🤙 3 · 🔥 1» с кликом для добавить/убрать; кнопка «+» открывает picker эмодзи (нативный input или библиотека вроде `emoji-mart`). Свои реакции подсвечены, можно убрать кликом.

#### Из миграции дизайна (пропущено или замокано)

Дизайн содержал ряд элементов, для которых нет данных на бэке либо это новая фича — оставлены на потом:

- **Геймификация: ачивки, XP, уровень** — на профиле в макете была секция «Достижения» (медальоны разблокированные/в процессе с прогресс-баром) и XP-полоса в hero. Пропущены полностью — нужны таблицы `achievements` + `user_achievements`, правила разблокировки и счётчик XP/level.
- **TweaksPanel — UI смены темы и акцента** — ThemeProvider + 5 акцентов уже работают, переключаются только через DevTools (`data-theme`, `--accent`). Нужна вынесенная панель (правый нижний угол по дизайну): тоггл dark/light, выбор акцента из 5 цветов, чекбоксы liquid-bg / grain / scanlines.
- **Шрифт-свитчер** — был на странице профиля (JetBrains Mono / Orbitron / Rajdhani / Share Tech Mono / Monocraft), удалён при миграции профиля. Должен вернуться в TweaksPanel, логика в `shared/lib/user-preferences.ts` ещё жива.
- **Live ticker в шапке** — лента «X оценил Y · 4.5★ · 2m назад» в подвале хедера. Дизайн готов (`.ticker`/`.ticker-row`/`.ticker-item`), но нужен endpoint `/api/recent-events` или WebSocket — взять последние ~20 действий (созданные/обновлённые отзывы) с usernames и drink names.
- **Email-поле и «Войти как гость (demo)»** — в auth-форме дизайна были `<input type="email">` и кнопка demo-входа. Пропущены — наш бэк аутентифицируется по `username + password`, demo-режима тоже нет. Email понадобится для верификации/сброса пароля (см. отдельный пункт).
- **Heatmap-вью каталога** — альтернативный режим к сетке: таблица «напиток × метрика» с цветовой тепловой картой. SortBar в дизайне имеет тоггл `grid`/`heatmap` — у нас только grid, тоггл не нарисован.
- **«AI: похожие по вкусовому профилю»** — на drink-странице блок назван AI, но фактически считает евклидово расстояние L2 по 6 метрикам. Если будет нужен настоящий контент-бейзед/коллаборативный — потребуется отдельный сервис.
- **«Популярное сейчас» вместо «AI: подобные вкусы» в SideRail** — в дизайне это была персонализированная рекомендация под пользователя («для marina_x» — топ-3 близких по профилю). У нас вместо неё топ-3 по числу отзывов — без персонализации.
- **Tier как сохраняемое поле в БД** — сейчас `tierFromRating()` вычисляет S/A/B/C/D из рейтинга. С админ-полем можно курировать вручную, не привязываясь к рейтингу (для tier-list страницы).
- **CTA «Сравнить» в hero напитка** — был в дизайне, но без `/compare` страницы не имеет смысла. Появится вместе с фичей сравнения напитков.
- **`joined`-дата пользователя на профиле** — в макете «с дд.мм.гггг», нет поля `created_at` на `User`. Нужно добавить миграцию.
- **`drink.can` colors (цвета 3D-банки)** — на каждый напиток в макете была уникальная цветовая раскраска банки (body/stripe/accent). Сейчас цвет вычисляется из blend-цвета метрик, остальное — мок. Если хотим уникальные банки — нужно `can_colors` поле или генератор от хеша id.
- **Footer на новый дизайн** — `.foot`/`.foot-left`/`.foot-logo`/`.foot-right`/`.foot-link` CSS уже добавлены ещё на вехе 1, но `widgets/footer/ui/Footer.tsx` ещё на старом Tailwind (`.glass rounded-xl`, `lucide-react` иконки, `text-neon-cyan`). Быстро переписать на новые классы.
- **HeaderSearchBar в `.search` контейнере** — в шапке сейчас поле поиска на старом Tailwind, обещали привести в порядок при миграции каталога, но отложили. Должно быть `.search` с иконкой+input+kbd, фильтр-кнопка ушла на каталог в SortBar (там уже `.filter-btn`).
- **`/tier` страница (tier-list)** — новая страница из дизайна (`frontendNew/page-tier.jsx`): группировка напитков по S/A/B/C/D с переключателем сорта (admin/users/blend). Сейчас tier есть только мини-баром на SideRail главной. Зависит от пункта «Tier как сохраняемое поле в БД» если хотим curate-режим, иначе работает от `tierFromRating(rating)`.
- **`/glossary` (`/sommelier`) страница** — новая страница из дизайна (`frontendNew/page-sommelier.jsx`): словарь энергетических терминов и метрик с примерами напитков. Контент-страница без backend-данных (либо статика, либо новая таблица `glossary_terms`).

### 🔧 Технический долг

- **Баг: `sort` и `noSugarOnly` не сбрасываются при смене маршрута** — `SearchResetter` в `catalog-search.tsx` сбрасывает `search`, `searchItems`, `filterOpen`, но забывает про `sort` и `noSugarOnly`. Если выбрать «Сначала дороже» и перейти на другую страницу — фильтр остаётся висеть.
- **`avgReview` использует `userReviews[0]` как базу** — в `ReviewsPage` средний отзыв считается через `{ ...userReviews[0], ...metrics }`, из-за чего в объект попадают `id`, `user_id`, `username` первого пользователя. Лучше собирать объект явно.
- **`deleteReviewAction` молча игнорирует ошибки** — если удаление не прошло (сеть, 403), пользователь не получит никакого сигнала, страница просто обновится как ни в чём не бывало.
- **Все 4 Google Fonts грузятся всегда** — в `layout.tsx` прописаны все шрифты сразу (~200 KB лишних запросов), независимо от того какой выбрал пользователь. Можно грузить только активный.
- **`FONT_INIT_SCRIPT` не валидирует значение шрифта** — inline-скрипт применяет `p.font` как есть, без проверки что это один из допустимых вариантов. Если в localStorage окажется мусор — он будет применён к `--font-sans`.
- **FilterPanel на старом Tailwind-дизайне** — всё остальное на странице каталога мигрировало на новый дизайн (`.sortbar`, `.card-glass`, `.stats`), а попап с фильтрами всё ещё `.glass rounded-2xl` с `framer-motion`. При расширении (метрик-слайдеры из дизайна) проще переделать целиком на `.rev-form`-подобный стиль.
- **`enrichDrinks` дважды на drink-странице** — `DrinkPage` вызывает `enrichDrinks(drinks, allReviews)` для SimilarRail, а `DrinkCatalog` ещё раз делает то же на главной. Если страницы соседствуют — можно вынести в провайдер/кэш.
- **Compatibility-shim `.glass` / `.glass-surface`** — оставлены в `globals.css` для оставшихся legacy-консьюмеров (admin/taste-map/auth и т.д.). Удалить после миграции остальных страниц.

### 💬 Обсуждения

- **Убрать из админки оставление отзывов** — администратор должен оставлять отзывы через обычный интерфейс, а не отдельную форму в панели управления.

---

## Архив

### Критичные проблемы

- ~~**CORS + credentials**~~ ✅ Список разрешённых origin'ов вынесен в `ALLOWED_ORIGINS`. По умолчанию только `localhost:3000`.
- ~~**Нет проверки владельца отзыва на бэкенде**~~ ✅ `PUT/DELETE /reviews/{id}` возвращает 403 если не владелец и не admin.
- ~~**Фронтенд не проверяет роль пользователя**~~ ✅ Серверная проверка роли на всех admin-страницах (коммит `90144ae`).
- ~~**Нет валидации пароля при регистрации**~~ ✅ `field_validator` в Pydantic v2: мин. 8 символов, заглавная, строчная, цифра. Валидация логина 3–50 символами и только `[a-zA-Z0-9_-]`.
- ~~**Порт БД торчит наружу**~~ ✅ PostgreSQL доступна только внутри Docker-сети `backend`, порт не публикуется.

### Средние замечания

- ~~**Нет ForeignKey в моделях**~~ ✅ `ForeignKey("energy_drinks.id")` и `ForeignKey("users.id")` с relationships.
- ~~**Загрузка изображений без валидации**~~ ✅ Разрешены только jpeg/png/webp/gif, лимит 5 МБ.
- ~~**Нет пагинации**~~ ✅ Бэкенд: `limit`/`offset` в `GET /energy-drinks/`. Фронт: 12 напитков / 6 отзывов на страницу.
- ~~**Дублирование bearer-заголовка**~~ ✅ `bearerHeaders(token)` вынесена в `@shared/api/http`.

### Идеи — реализовано

- ~~**Удаление собственного отзыва**~~ ✅ Реализовано.
- ~~**Рейтинг в каталоге**~~ ✅ Звёздный рейтинг + акцентный цвет карточки на основе метрик.
- ~~**Кнопка «Наверх»**~~ ✅ Появляется при скролле вверх после 300px.
- ~~**Footer**~~ ✅ Название проекта, год, кафедра, ссылки на GitHub/Email.
- ~~**CTA вместо прочерка**~~ ✅ «Оцените первым!» у карточек без рейтинга.
- ~~**Иконка вместо бейджа «Без сахара» на мобильных**~~ ✅ `CandyOff` на `< sm`, текст на `sm+`.

### Фидбек — закрыто

- ~~**URL `/reviews?id=16` → `/drinks?id=16`**~~ ✅ Роут переименован.
- ~~**Пагинация (Слава)**~~ ✅ Реализована (постраничная, 12/6).

### Реализовано в этой сессии

- ~~**Убрать `review.rating` из фронта**~~ ✅ Бэк дропнул колонку (миграция `585053038cea`); фронт везде вычисляет `calcRating(metrics)`, payload без поля `rating` (коммит `3092153`).
- ~~**Добавить в админку поле для загрузки фото**~~ ✅ Форма `DrinkForm` заменила текстовое поле URL на file-upload с превью и кнопкой очистки.
- ~~**Исправить обрезание строки поиска**~~ ✅ Placeholder сокращён до «Поиск…».
- ~~**Rate limiter на бэкенд**~~ ✅ `slowapi` in-memory: login 10/мин, register 5/мин, reviews 10/мин. Фронт обрабатывает HTTP 429 через `RateLimitError`.
- ~~**Яндекс Метрика**~~ ✅ Счётчик 109003264 подключён через `next/script` (`afterInteractive`), webvisor + clickmap включены.
- ~~**Поиск на странице отзывов**~~ ✅ Исправлен сломанный маршрут `/reviews` → `/drinks` в `HeaderSearchBar`.
- ~~**Отзыв администратора на странице напитка**~~ ✅ Администратор видит своё «Ваш администраторский отзыв» с кнопками редактирования; кнопка «Оставить отзыв» не показывается если отзыв уже есть.
- ~~**Автооткрытие формы отзыва из профиля**~~ ✅ При выборе напитка через «Добавить» в профиле форма отзыва открывается автоматически (`?review=1`).
