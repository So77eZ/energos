# Замечания и предложения по проекту Energos

## Критичные проблемы

### 1. ~~CORS + credentials~~ ✅ Исправлено (`backend/main.py`)

`allow_origins=["*"]` вместе с `allow_credentials=True` — нарушение стандарта CORS. Браузер запрещает это сочетание, и либо credentials не работают, либо сервер уязвим к CSRF.

**Что сделано:** список разрешённых origin'ов вынесен в переменную окружения `ALLOWED_ORIGINS`. По умолчанию разрешены только `http://localhost:3000` и `http://127.0.0.1:3000`. В продакшне задать: `ALLOWED_ORIGINS=https://yourdomain.com`.

### 2. ~~Нет проверки владельца отзыва на бэкенде~~ ✅ Реализовано

Проверка `user_id == current_user.id` присутствует в `PUT /reviews/{id}` и `DELETE /reviews/{id}` — возвращает 403 если не владелец и не admin.

### 3. ~~Фронтенд не проверяет роль пользователя~~ ✅ Исправлено

Реализована серверная проверка роли на всех admin-страницах и страницах отзывов (коммит `90144ae`).

### 4. ~~Нет валидации пароля при регистрации~~ ✅ Исправлено

`UserCreate` теперь проверяет: минимум 8 символов, наличие заглавной и строчной буквы, цифры и спецсимвола (`field_validator` в Pydantic v2). Ошибки передаются на фронтенд и отображаются в форме регистрации. Логин ограничен 3–50 символами.

---

## Средние замечания

### 5. ~~Нет ForeignKey в моделях~~ ✅ Исправлено

`energy_drink_id` и `user_id` объявлены с `ForeignKey("energy_drinks.id")` и `ForeignKey("users.id")`, relationships настроены.

### 6. ~~Загрузка изображений без валидации типа файла~~ ✅ Исправлено

Разрешены только `image/jpeg`, `image/png`, `image/webp`, `image/gif`; размер ограничен 5 МБ. Возвращает 400 с понятным сообщением.

### 7. ~~Нет пагинации~~ ✅ Реализовано

Бэкенд: `GET /energy-drinks/` принимает `limit` и `offset`. Фронтенд: клиентская пагинация в каталоге (12 на страницу) и на странице отзывов (6 на страницу).

### 8. ~~Дублирование bearer-заголовка в API-клиентах~~ ✅ Исправлено

`bearerHeaders(token)` вынесена в `@shared/api/http`, импортируется в `drinkApi`, `reviewApi`, `authApi`.

---

## Идеи для развития

- **Сортировка и фильтрация отзывов** по рейтингу и дате — сейчас отсутствует.
- ~~**Удаление собственного отзыва** пользователем — кнопка не реализована.~~ ✅ Реализовано.
- **Карта вкусов:** отображает только 2 метрики из 6 — можно добавить выбор осей X/Y.
- ~~**Рейтинг в каталоге:** карточки напитков не показывают звёздный рейтинг из отзывов.~~ ✅ Реализовано, также добавлен акцентный цвет карточки на основе смешения цветов метрик.
- **Сброс пароля / верификация email** — стандарт для пользовательских платформ.
- **Сравнение напитков** — режим side-by-side по всем метрикам.
- ~~**Слайдер наверх**~~ ✅ Реализован. Кнопка появляется при скролле вверх после 300px, исчезает при скролле вниз.
- **Расширенный фильтр** — при нажатии на кнопку фильтрации, всплывает окно с расширенной фильтрацией по параметрам напитка, а не только с сортировкой.
- ~~**Сделать боттом**~~ ✅ Реализован. Footer с названием проекта, годом, кафедрой и ссылками на GitHub/Email.

## Пользовательский фидбек

Саня бэкенд:
~~Изменить url вида `/reviews?id=16` на `/drinks?id=16`~~ ✅ Исправлено
Добавить в админку поле для добавления фотки
Убрать из админки оставление отзывов
Добавить какой-то способ обратной связи (ссылка на личку тг или тг бота например)
Заявки на добавление энергосов (Например название энергоса + ссылка на референс (опционально, вдруг энергос какой-то вообще неизвестный)
Парсинг актуальных цен

Слава ширинка:
Всё круто, так и оставляем;
Референс: <https://risazatvorchestvo.com/>
<https://shop.tastycoffee.ru/coffee/rwanda-kilimbi?tab=recipes>
Пересылка:
    ~~*измените стандартный порт базы на кастомный, поставьте хороший пароль, и сделай чтобы порт не торчал наружу~~ ✅ Порт БД не публикуется, доступна только внутри Docker-сети
    ~~*чтобы база была доступна только с локального компа, чтобы только бекенд к ней мог обращаться с того же локального компа~~ ✅ Реализовано через Docker-сеть `backend`
    *фронт кидает запрос на бек, порт бека открыт для всех;
    *бек обращается к базе на том же компе, порты базы закрыты наружу
    *рейтлимитер можно на бек поставить чтобы от спама защититься, но на фронте его поддержать нужно
    *фильтров чет мало и сортировок - мож я по дороговизне хочу
    ~~*ещё хз какая у вас пагинация, но я надеюсь бесконечная~~ ✅ Пагинация реализована (12 напитков / 6 отзывов на страницу)
    *а какой стейт менеджер? (редакс, zustand)
    *кеша наверное на фронт можно закинуть
    *(По приколу)  можно например новые цены отправлять по вебсокету (сделать фцию обновления цен от рандома)
    *добавьте кстати яндекс метрику, эт на фронте делается двумя строчками прям из их документации;
    *Изменение надписи в строке поиска на главной странице - обрубается как "Поиск напи" комментарий - поиск напи, напи что, написюн, или кто, если не влезает оставь просто "Поиск", а если влезает, то пусть влезает полностью в десктопе например;
    *Вместе прочерка с отсуствующей оценокой у карточки энергетика сделать CTA (call to action) (справа от звездочки надпись - "Оцените первым!");
    *Не хватает полнотекстового поиска или что-то в этом духе (монстер, монстэр монстр monstr monster), использовать pg_trgm;
    *и англо-русский словарь я бы добавил: пишешь например "бэрн", а тебе предлагают "Burn", тоже сделать в духе "Если искомое слово более чем на 90% напоминает Берн/Бёрн/Бёрн/Брн/Бэрн, то дополнительно искать слова похожие на "Burn".
